#include "C_WeaponBaseItem.h"

using namespace cheat;
using namespace csgo;

bool& C_WeaponBaseItem::m_bRedraw( )
{
#ifdef CHEAT_NETVARS_UPDATING
	return *(bool*)nullptr;
#else
	static const auto offset = netvars::get_ptr( )->at("C_WeaponBaseItem", "m_bRedraw");
	auto addr = nstd::address(this).add(offset);
	return addr.ref<bool>( );
#endif
}
