#include "settings_shared.h"

using namespace cheat;

cheat::settings_shared::settings_shared( )
{
	//impl_ = std::make_unique<impl>( );
}

cheat::settings_shared::~settings_shared( )
{
}

//settings_shared::ostream& cheat::operator<<(settings_shared::ostream& stream, const settings_shared& shared)
//{
//	shared.save(stream);
//	return stream;
//}
//
//settings_shared::istream& cheat::operator>>(settings_shared::istream& stream, settings_shared& shared)
//{
//	shared.load(stream);
//	return stream;
//}
