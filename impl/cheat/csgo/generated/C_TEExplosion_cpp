#include "C_TEExplosion.h"
#include "cheat/netvars/netvars.h"
#include <nstd/address.h>

#include "cheat/csgo/Vector.hpp"

using namespace cheat::csgo;

int& C_TEExplosion::m_nModelIndex( )
{
	static const auto offset = netvars::get( )->at("C_TEExplosion", "m_nModelIndex");
	auto addr = nstd::address(this).add(offset);
	return addr.ref<int>( );
}
float& C_TEExplosion::m_fScale( )
{
	static const auto offset = netvars::get( )->at("C_TEExplosion", "m_fScale");
	auto addr = nstd::address(this).add(offset);
	return addr.ref<float>( );
}
int& C_TEExplosion::m_nFrameRate( )
{
	static const auto offset = netvars::get( )->at("C_TEExplosion", "m_nFrameRate");
	auto addr = nstd::address(this).add(offset);
	return addr.ref<int>( );
}
int& C_TEExplosion::m_nFlags( )
{
	static const auto offset = netvars::get( )->at("C_TEExplosion", "m_nFlags");
	auto addr = nstd::address(this).add(offset);
	return addr.ref<int>( );
}
cheat::csgo::Vector& C_TEExplosion::m_vecNormal( )
{
	static const auto offset = netvars::get( )->at("C_TEExplosion", "m_vecNormal");
	auto addr = nstd::address(this).add(offset);
	return addr.ref<cheat::csgo::Vector>( );
}
unsigned char& C_TEExplosion::m_chMaterialType( )
{
	static const auto offset = netvars::get( )->at("C_TEExplosion", "m_chMaterialType");
	auto addr = nstd::address(this).add(offset);
	return addr.ref<unsigned char>( );
}
int& C_TEExplosion::m_nRadius( )
{
	static const auto offset = netvars::get( )->at("C_TEExplosion", "m_nRadius");
	auto addr = nstd::address(this).add(offset);
	return addr.ref<int>( );
}
int& C_TEExplosion::m_nMagnitude( )
{
	static const auto offset = netvars::get( )->at("C_TEExplosion", "m_nMagnitude");
	auto addr = nstd::address(this).add(offset);
	return addr.ref<int>( );
}
