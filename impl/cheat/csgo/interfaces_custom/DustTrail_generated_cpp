float& DustTrail::m_SpawnRate( )
{
	static const auto offset = cheat::get_netvar_offset("DustTrail", "m_SpawnRate");
	auto addr = nstd::mem::address(this).add(offset);
	return addr.ref( );
}

cheat::csgo::Vector& DustTrail::m_Color( )
{
	static const auto offset = cheat::get_netvar_offset("DustTrail", "m_Color");
	auto addr = nstd::mem::address(this).add(offset);
	return addr.ref( );
}

float& DustTrail::m_ParticleLifetime( )
{
	static const auto offset = cheat::get_netvar_offset("DustTrail", "m_ParticleLifetime");
	auto addr = nstd::mem::address(this).add(offset);
	return addr.ref( );
}

float& DustTrail::m_StopEmitTime( )
{
	static const auto offset = cheat::get_netvar_offset("DustTrail", "m_StopEmitTime");
	auto addr = nstd::mem::address(this).add(offset);
	return addr.ref( );
}

float& DustTrail::m_MinSpeed( )
{
	static const auto offset = cheat::get_netvar_offset("DustTrail", "m_MinSpeed");
	auto addr = nstd::mem::address(this).add(offset);
	return addr.ref( );
}

float& DustTrail::m_MaxSpeed( )
{
	static const auto offset = cheat::get_netvar_offset("DustTrail", "m_MaxSpeed");
	auto addr = nstd::mem::address(this).add(offset);
	return addr.ref( );
}

float& DustTrail::m_MinDirectedSpeed( )
{
	static const auto offset = cheat::get_netvar_offset("DustTrail", "m_MinDirectedSpeed");
	auto addr = nstd::mem::address(this).add(offset);
	return addr.ref( );
}

float& DustTrail::m_MaxDirectedSpeed( )
{
	static const auto offset = cheat::get_netvar_offset("DustTrail", "m_MaxDirectedSpeed");
	auto addr = nstd::mem::address(this).add(offset);
	return addr.ref( );
}

float& DustTrail::m_StartSize( )
{
	static const auto offset = cheat::get_netvar_offset("DustTrail", "m_StartSize");
	auto addr = nstd::mem::address(this).add(offset);
	return addr.ref( );
}

float& DustTrail::m_EndSize( )
{
	static const auto offset = cheat::get_netvar_offset("DustTrail", "m_EndSize");
	auto addr = nstd::mem::address(this).add(offset);
	return addr.ref( );
}

float& DustTrail::m_SpawnRadius( )
{
	static const auto offset = cheat::get_netvar_offset("DustTrail", "m_SpawnRadius");
	auto addr = nstd::mem::address(this).add(offset);
	return addr.ref( );
}

bool& DustTrail::m_bEmit( )
{
	static const auto offset = cheat::get_netvar_offset("DustTrail", "m_bEmit");
	auto addr = nstd::mem::address(this).add(offset);
	return addr.ref( );
}

float& DustTrail::m_Opacity( )
{
	static const auto offset = cheat::get_netvar_offset("DustTrail", "m_Opacity");
	auto addr = nstd::mem::address(this).add(offset);
	return addr.ref( );
}

